jar {
    manifest {
        attributes 'Implementation-Title': 'requirements as code - pizza volume calculator',
                   'Implementation-Version':archiveVersion
    }
}

dependencies {
	implementation project(':requirementsascodecore')
}

