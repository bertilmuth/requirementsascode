jar {
    manifest {
        attributes 'Implementation-Title': 'requirements as code - cross cutting concerns',
                   'Implementation-Version': version
    }
}

dependencies {
	implementation project(':requirementsascodecore')
}

