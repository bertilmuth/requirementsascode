jar {
  manifest {
    attributes 'Implementation-Title': 'requirements as code - actor example', 'Implementation-Version':archiveVersion
  }
}

dependencies {
  implementation project(':requirementsascodecore')
  testImplementation "org.mockito:mockito-core:3.+"
}

